#include "GenerateUuid.h"
#include <iostream>


/*
auto GenerateUuid::generate()
{
	std::random_device rd;
	std::mt19937 gen(rd());

	uuids::uuid_random_generator uuid_gen(gen);

	uuids::uuid id = uuid_gen();

	std::string uuid = uuids::to_string(id);

	return uuid;
}
*/